import { ProfesorShowPipe } from './profesor-show.pipe';

describe('ProfesorShowPipe', () => {
  it('create an instance', () => {
    const pipe = new ProfesorShowPipe();
    expect(pipe).toBeTruthy();
  });
});
