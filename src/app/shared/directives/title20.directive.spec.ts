import { Title20Directive } from './title20.directive';

describe('Title20Directive', () => {
  it('should create an instance', () => {
    const directive = new Title20Directive();
    expect(directive).toBeTruthy();
  });
});
